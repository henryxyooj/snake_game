how pygame draws
-display surface
    -like a blank canvas, only one per game
-regular surface
    -another type of blank canvas, we can have as many per game
    -displaying text over another canvas, etc
-rect
    -used for positioning, collision detection, and for drawing objects

loading an image
-pygame.image.load()
